Push bằng các lệnh sao:
vào thư mục có folder ".git" -> mở cmd
chạy lệnh 
--------------------------------------
git add Assets             => Enter
git add Packages           => Enter
git add ProjectSettings    => Enter

git commit -m 'conten'     => Enter
git push                   => Enter
--------------------------------------
xong!
